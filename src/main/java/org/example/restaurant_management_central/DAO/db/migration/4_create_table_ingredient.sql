create table ingredient(
                           id int primary key,
                           name varchar,
                           last_modification timestamp,
                           unit_price decimal,
                           unity unit
);
